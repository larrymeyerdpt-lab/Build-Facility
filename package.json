{
  "name": "build-facility-tracking",
  "version": "1.1.0",
  "description": "BUILD Facility Course Tracking — Postgres-backed booking & capacity dashboard",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  }
}
